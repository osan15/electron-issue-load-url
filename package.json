{
  "name": "electron-load-url-issue",
  "version": "1.0.0",
  "description": "",
  "main": "app/main.js",
  "scripts": {
    "postinstall": "install-app-deps",
    "start": "npm install && electron .",
    "clean": "rm -rf build",
    "pack": "npm run clean && electron-builder --dir --windows",
    "build": "npm run clean && electron-builder --windows && gulp toScr"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^12.0.1",
    "electron-builder": "^22.10.5",
    "gulp": "^4.0.2",
    "gulp-rename": "^2.0.0"
  },
  "dependencies": {
    "electron-log": "^4.3.2"
  }
}
